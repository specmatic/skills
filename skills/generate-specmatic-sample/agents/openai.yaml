interface:
  display_name: "Generate Specmatic Sample"
  short_description: "Generate Specmatic contract-test samples"
  default_prompt: "Use $generate-specmatic-sample to generate a Backend REST, gRPC, GraphQL, AsyncAPI, or SOAP sample with Specmatic contract tests."

policy:
  allow_implicit_invocation: true
